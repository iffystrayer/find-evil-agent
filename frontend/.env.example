# Find Evil Agent Frontend Configuration
# Copy this file to .env.local for local development

# Backend API URL
VITE_API_BASE_URL=http://localhost:18000

# API key for authenticated requests. Match one of the keys in the backend's
# API_KEYS env. Leave empty when the backend has auth disabled (local dev).
# VITE_API_KEY=
